function solve() {
 
 const person = {

   firstName: "Peter",
   lastName: "Pan",
   age: "20"
 }

   let element = Object.keys(person)
  

  element.forEach((x) => {
    console.log(`${x} -> ${person[x]}`)
     
  })

}


->firstName -> Peter
lastName -> Pan
age -> 20
