function dayOfWeek(day) {
    let nameOfDay = ["Monday", "Tuesday",
    "Wednesday", "Thursday","Friday","Saturday","Sunday"];
    if(day >= 1 && day <= 7) {
        return nameOfDay[day-1]
    } else {
        return "Invalid day!"
    }
}


console.log(dayOfWeek(3))
