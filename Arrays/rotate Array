function solve(arr) {
 let step= +arr.pop();

 for(let i = 0; i< step;i++) {
    let el = arr.pop()
     arr.unshift(el)
 }
return arr.join(" ")
}


console.log(
    solve(['1','2','3','4','2'])
)

->3 4 1 2
