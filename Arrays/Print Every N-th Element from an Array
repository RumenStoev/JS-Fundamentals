function solve(arr) {

    let element  = +arr.pop();
    for(let i = 0; i< arr.length; i+=element) {
        console.log(arr[i])
    }
}


console.log(
    solve([ '5', '20', '31', '4', '20', '2' ])
)
->5
31
20
