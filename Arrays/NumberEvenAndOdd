function solve(arr) {
let sum = 0;
let sum1 = 0;
for(let i = 0; i< arr.length;i++) {
  arr[i] = Number(arr[i])
}
 for(let num of arr) {
   if(num %2 === 0) {
      sum += num;
   } else {
   sum1 += num
   }
 }

return sum - sum1
}

console.log(
  solve(['1','2','3','4','5','6'])
)
