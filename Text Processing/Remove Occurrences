function solve(key,text) {
   let result = text.replace(key,"");
   while (result.indexOf(key) != -1) {
       result = result.replace(key,"")
   }
   return result

}
function removeOccurences(str, str2) {
    let allWord = str2;
    let searchWord = str;

    while (allWord.includes(searchWord)) {
        allWord = allWord.replace(searchWord, "")

    }

    return allWord
}

console.log(
    solve("ice","kicegiciceeb")
)
->kgb :D
